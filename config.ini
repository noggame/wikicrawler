[identifier]
title   = #
list    = -
table   = |
code    = `
description = `

[filter]
available_tag       = h1, h2, h3, h4, h5, h6, p, ul, ol, dl, table, pre
exclude_tag         = sup, figure
exclude_class       = mw-editsection, #mwe-math-mathml-a11y
exclude_passage     = 각주, 외부 링크, #참고, #참고 문헌, #같이 보기